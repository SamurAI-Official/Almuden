"""Risk agent (deferred).

LLM-driven risk management agent. Implemented in a later phase.
"""
from __future__ import annotations

import logging

log = logging.getLogger(__name__)


class RiskAgent:
    """Stub — raises if used before implementation."""

    def __init__(self, *args, **kwargs) -> None:
        log.warning("RiskAgent is not yet implemented (deferred)")
