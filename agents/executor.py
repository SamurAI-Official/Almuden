"""Executor agent (deferred).

LLM-driven trade execution agent. Implemented in a later phase.
"""
from __future__ import annotations

import logging

log = logging.getLogger(__name__)


class ExecutorAgent:
    """Stub — raises if used before implementation."""

    def __init__(self, *args, **kwargs) -> None:
        log.warning("ExecutorAgent is not yet implemented (deferred)")
