"""Analyst agent (deferred).

LLM-driven market analysis agent. Implemented in a later phase.
"""
from __future__ import annotations

import logging

log = logging.getLogger(__name__)


class AnalystAgent:
    """Stub — raises if used before implementation."""

    def __init__(self, *args, **kwargs) -> None:
        log.warning("AnalystAgent is not yet implemented (deferred)")
