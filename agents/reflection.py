"""Reflection agent (deferred).

LLM-driven self-reflection / strategy review agent. Implemented in a later phase.
"""
from __future__ import annotations

import logging

log = logging.getLogger(__name__)


class ReflectionAgent:
    """Stub — raises if used before implementation."""

    def __init__(self, *args, **kwargs) -> None:
        log.warning("ReflectionAgent is not yet implemented (deferred)")
