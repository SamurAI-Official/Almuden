"""Memory agent (deferred).

LLM-driven memory management agent. Implemented in a later phase.
"""
from __future__ import annotations

import logging

log = logging.getLogger(__name__)


class MemoryAgent:
    """Stub — raises if used before implementation."""

    def __init__(self, *args, **kwargs) -> None:
        log.warning("MemoryAgent is not yet implemented (deferred)")
