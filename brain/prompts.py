"""Prompt templates (deferred).

System and user prompt templates for the LLM agents. Implemented in a later phase.
"""
from __future__ import annotations

import logging

log = logging.getLogger(__name__)


class PromptLibrary:
    """Stub — raises if used before implementation."""

    def __init__(self, *args, **kwargs) -> None:
        log.warning("PromptLibrary is not yet implemented (deferred)")
