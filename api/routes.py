"""REST API routes (deferred).

FastAPI routes for external control and monitoring. Implemented in a later phase.
"""
from __future__ import annotations

import logging

log = logging.getLogger(__name__)


class APIRouter:
    """Stub — raises if used before implementation."""

    def __init__(self, *args, **kwargs) -> None:
        log.warning("APIRouter is not yet implemented (deferred)")
