"""Long-term memory (deferred).

Persistent memory store for the agent layer. Implemented in a later phase.
"""
from __future__ import annotations

import logging

log = logging.getLogger(__name__)


class LongTermMemory:
    """Stub — raises if used before implementation."""

    def __init__(self, *args, **kwargs) -> None:
        log.warning("LongTermMemory is not yet implemented (deferred)")
