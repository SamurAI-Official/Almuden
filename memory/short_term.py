"""Short-term memory (deferred).

In-memory working memory for the agent layer. Implemented in a later phase.
"""
from __future__ import annotations

import logging

log = logging.getLogger(__name__)


class ShortTermMemory:
    """Stub — raises if used before implementation."""

    def __init__(self, *args, **kwargs) -> None:
        log.warning("ShortTermMemory is not yet implemented (deferred)")
