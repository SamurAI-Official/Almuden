"""RAG pipeline (deferred).

Retrieval-augmented generation pipeline for the agent layer. Implemented in a later phase.
"""
from __future__ import annotations

import logging

log = logging.getLogger(__name__)


class RAGPipeline:
    """Stub — raises if used before implementation."""

    def __init__(self, *args, **kwargs) -> None:
        log.warning("RAGPipeline is not yet implemented (deferred)")
