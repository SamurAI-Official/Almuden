"""Browser tool (deferred).

Headless browser for scraping exchange announcements. Implemented in a later phase.
"""
from __future__ import annotations

import logging

log = logging.getLogger(__name__)


class Browser:
    """Stub — raises if used before implementation."""

    def __init__(self, *args, **kwargs) -> None:
        log.warning("Browser is not yet implemented (deferred)")
