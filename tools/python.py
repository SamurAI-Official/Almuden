"""Python execution tool (deferred).

Sandboxed Python execution for LLM agents. Implemented in a later phase.
"""
from __future__ import annotations

import logging

log = logging.getLogger(__name__)


class PythonTool:
    """Stub — raises if used before implementation."""

    def __init__(self, *args, **kwargs) -> None:
        log.warning("PythonTool is not yet implemented (deferred)")
